/* ==========================================================================
   Pesito — Lógica de comportamiento (JavaScript)
   ==========================================================================
   FASE 1: este archivo todavía no tiene lógica implementada.
   La interactividad (menú móvil, validación de formularios en tiempo real,
   registro de movimientos, cálculo de metas, gráficos, etc.) se agrega en
   la FASE 3 del proyecto, siguiendo estas reglas obligatorias:

   - Nada de atributos inline (onclick, onsubmit, etc.) en el HTML.
   - Toda la inicialización que toque el DOM arranca en window 'load'.
   - Los listeners se asignan acá, nunca en el HTML.

   Estructura que se va a usar en la Fase 3:

   window.addEventListener('load', iniciar);

   function iniciar() {
     // referencias a elementos + addEventListener acá
   }
   ========================================================================== */
